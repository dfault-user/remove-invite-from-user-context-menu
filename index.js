const { Plugin } = require('powercord/entities')

module.exports = class RemoveInviteFromUserContextMenu extends Plugin {
    async startPlugin () {
	console.log("you smell like you farted");
	console.log("farted");
        this.loadStylesheet('style.css');
    }
}
